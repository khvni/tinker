---
type: session
date: 2026-04-21
ticket: TIN-75
pillar: M8
branch: khvni/feat/m8/tin-75-user-type
pr: 27
---

# TIN-75 — M8.2 User type in `@tinker/shared-types`

## Outcome

Draft PR opened and moved to ready-for-review after verification. Linear moved from Backlog to In Progress during execution; next step is In Review with PR link attached.

- Branch: `khvni/feat/m8/tin-75-user-type`
- Commit: `033d45b`
- PR: https://github.com/khvni/tinker/pull/27
- Spec: `agent-knowledge/features/28-mvp-identity.md`

## What changed

- `packages/shared-types/src/session.ts`
  - Expanded `User` from placeholder `{ id: string }` to MVP identity contract:
    - `id`
    - `provider`
    - `providerUserId`
    - `displayName`
    - `avatarUrl?`
    - `email?`
    - `createdAt`
    - `lastSeenAt`
- `agent-knowledge/context/tasks.md`
  - Advanced M8.2 row to `review` with PR #27.

## Reviewer pass

- Requirement fit: exact ticket scope only. No spill into `users` table, auth sidecar, or SSO provider wiring.
- Elon 5-step: no new abstractions, wrappers, or helpers. Direct type expansion only.
- Buchan Glass / CLAUDE.md compliance:
  - no UI work, so no D14/D23 token risk
  - no `any`
  - no `@ts-ignore`
  - no `dockview-react`
  - strict TS preserved

## Verification

- `pnpm --filter @tinker/shared-types typecheck` — green
- `pnpm -r typecheck` — green
- `pnpm -r test` — green

## Notes for follow-up

- `packages/shared-types/src/sso.ts` still models only `google` and `github`. Left untouched here because TIN-75 was scoped to the shared `User` contract only.
