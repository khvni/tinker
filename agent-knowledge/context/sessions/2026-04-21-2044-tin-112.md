---
tags: [session, tin-112, playbook, shared-types]
---

# TIN-112 — skill schema + frontmatter spec

## Scope

User explicitly approved a D25 override for this post-MVP Playbook task. Kept work limited to the schema/parser surface named in the ticket.

## Shipped

- `packages/shared-types/src/skill.ts`
  - Added canonical Playbook skill schema types: `SkillAuthor`, `SkillRole`, `SkillSpecFrontmatter`, `SkillSpec`.
  - Extended legacy `SkillFrontmatter` and `SkillDraft` additively with optional canonical fields (`id`, `title`, `role`, `version`, `author`) to preserve compatibility.
- `packages/shared-types/src/skill.test-d.ts`
  - Added compile-time coverage for canonical frontmatter requirements and additive compatibility.
- `packages/memory/src/skill-parser.ts`
  - Parser now normalizes canonical skill metadata into a typed `spec`.
  - Supports `id/title/role/tools/version/author/body` while still loading legacy `name/description` files.
  - `draftToSkillContent()` now emits canonical metadata for newly authored skills without breaking legacy consumers.
- `packages/memory/src/skill-parser.test.ts`
  - Added coverage for canonical frontmatter parsing, author normalization, default role/version inference, and draft serialization.
- `agent-knowledge/features/02-playbook-skill-marketplace.md`
  - Updated the Skill File Format section to document the canonical frontmatter spec and the legacy-compatibility note.

## Verification

- `pnpm install --frozen-lockfile`
- `pnpm -r typecheck`
- `pnpm -r test`

## PR

- Draft PR: `feat(shared-types): add skill schema + frontmatter spec (TIN-112)` — PR #52

## Notes

- No matching atomic row exists in `agent-knowledge/context/tasks.md` for `TIN-112`; this is post-MVP feature work under the deferred Playbook umbrella, so task-table updates need to be handled as a feature-level note rather than an MVP row flip.
