---
type: session
date: 2026-04-21
ticket: TIN-66
branch: khvni/tin-66
---

# TIN-66 — M7.1 Strip `opencode.json`

## Shipped

Reduced `opencode.json` to the three MVP MCPs only: `exa`, `qmd`, and `smart-connections`. Removed deferred MCP entries for `better-auth`, `github`, and `linear`. Removed the `SMART_VAULT_PATH` config placeholder from `smart-connections`; Tauri already injects that env at OpenCode sidecar spawn time.

## Reasoning

D25 and the M7 feature spec narrow MVP to three auth-free MCPs. This ticket was pure delete/simplify work: remove deferred integrations instead of carrying compatibility scaffolding. Dropping the config-local env placeholder keeps `opencode.json` free of user-supplied env requirements while preserving spawn-time env wiring already implemented in `apps/desktop/src-tauri/src/commands/opencode.rs`.

## Verification

- `jq '.mcp | {keys: keys, count: (keys | length), enabled: map(.enabled)}' opencode.json`
- `pnpm -r typecheck`
- `pnpm -r test`
- Draft PR: `#26` (`feat(m7): strip deferred MCPs (TIN-66)`)

## Files

- `opencode.json`
- `agent-knowledge/context/tasks.md`
- `agent-knowledge/context/sessions/2026-04-21-1724-tin-66.md`
