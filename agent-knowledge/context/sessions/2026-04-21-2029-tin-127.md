---
type: session
tags: [session, TIN-127, coach, role-profile, shared-types]
---

# TIN-127 — Coach role profile foundation

Contributor: khvni
Agent: Codex

## Scope
- Evaluated `TIN-127` ("Coach — role profile + tool map") for implementation.
- Read required context before coding: `CLAUDE.md`, `tinker-prd.md`, `agent-knowledge/product/decisions.md` with focus on D25, `agent-knowledge/README.md`, `agent-knowledge/context/tasks.md`, recent session logs, `agent-knowledge/features/05-coach-skill-discovery.md`, and `agent-knowledge/features/02-playbook-skill-marketplace.md`.
- Landed the smallest unblocker slice only: shared `RoleProfile` contract plus inference from connected tool ids + installed skill history.

## Changes
- Renamed branch to `khvni/tin-127-agent-flow` per workspace instruction.
- Added `packages/shared-types/src/coach.ts` and exported `RoleProfile` through `@tinker/shared-types`.
- Extended `SkillStore` with `getRoleProfile(connectedTools)`.
- Added `packages/memory/src/role-profile.ts` pure inference module and `packages/memory/src/role-profile.test.ts`.
- Wired `createSkillStore()` to infer the role profile from stored skills.
- Added a knowledge-base note to `agent-knowledge/features/05-coach-skill-discovery.md` documenting the narrow `TIN-127` exception.
- Set Linear `TIN-127` to `In Progress` and commented branch name during initial scope check.
- Filed Linear follow-up `TIN-171` during the scope check; kept as historical record of the D25 conflict.

## Decisions Made
- [2026-04-21] Treated user instruction as an explicit exception to D25 for `TIN-127`.
- [2026-04-21] Kept scope narrow: no Coach UI, no recommendation surfaces, no Playbook dependency reopen. Only type contract + inference API.
- [2026-04-21] Approximated `frequentedSkills` from active skills first, then install recency, because per-skill usage telemetry does not exist yet.

## Knowledge Base Updates
- Updated `agent-knowledge/features/05-coach-skill-discovery.md` so future agents know `TIN-127` is an approved unblocker slice, not a full feature reopen.
- Wrote this session log with both the initial blocker context and the final shipped scope.

## Open Questions / Handoff
- Future Coach code should decide whether `roleLabel` heuristics belong in `@tinker/memory` long-term or move into a dedicated `packages/coach` package once that package exists.
- If real skill-usage telemetry lands later, replace the active+install-recency approximation for `frequentedSkills`.

## Gaps Surfaced
- The post-MVP task table has no atomic row for `TIN-127`, so there was nothing in `agent-knowledge/context/tasks.md` to flip to `review`.
