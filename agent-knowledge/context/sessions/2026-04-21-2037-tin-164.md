---
type: session
tags: [session, TIN-164, release, macos, signing, notarization]
---

# Session 2026-04-21 — TIN-164 macOS release signing
Contributor: khvni
Agent: Codex

## Scope
- Deliver TIN-164 to PR-ready state.
- Implement signed macOS `.app` + `.dmg` release plumbing with notarization and stapling on GitHub Actions.

## Changes
- Added `.github/workflows/release-macos.yml` for manual/tagged macOS release builds on `macos-latest`.
- Added `apps/desktop/scripts/notarize-macos-release.sh` to locate artifacts, notarize the DMG, staple both bundles, and validate the stapled output.
- Added `docs/distribution-signing.md` and linked it from `docs/README.md` so operators have a concrete secret/setup guide.
- Added `agent-knowledge/reference/macos-release-signing.md` plus `agent-knowledge/specs/2026-04-21-macos-release-signing-design.md` so future agents have local context for the release flow.
- Opened PR #47 and moved Linear TIN-164 to `In Review`.

## Decisions Made
- [2026-04-21] Kept default Tauri build config unchanged; release workflow overrides bundle targets at CLI time with `--bundles app,dmg` to avoid changing normal local build behavior.
- [2026-04-21] Used explicit post-build DMG notarization instead of adding a larger release framework because current Tauri in-repo still needs a manual DMG notarization step.
- [2026-04-21] Built universal macOS artifacts (`universal-apple-darwin`) so one release output covers Apple Silicon and Intel.

## Knowledge Base Updates
- Added `agent-knowledge/reference/macos-release-signing.md` to document the current Tauri DMG notarization gap and required secrets.
- Added `agent-knowledge/specs/2026-04-21-macos-release-signing-design.md` as the in-repo execution contract linked from PR #47.
- Updated `agent-knowledge/context/tasks.md` with a post-MVP row for TIN-164 in `review`.

## Open Questions / Handoff
- Workflow has been linted and repo tests pass, but the GitHub Actions job still needs a real run with Apple secrets configured in the repo.
- If future maintainers upgrade to a Tauri release that notarizes DMG artifacts natively, delete the explicit `notarytool submit` step instead of layering on another abstraction.

## Gaps Surfaced
- Repo had no operator-facing release/signing doc before this session.
- Post-MVP infra tickets were not tracked in `agent-knowledge/context/tasks.md`; added a minimal in-flight row for TIN-164.
