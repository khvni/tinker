---
type: session
date: 2026-04-22
topic: TIN-63 + TIN-64 + TIN-65 flat-file memory MVP
---

# Session — Flat-file memory MVP

## Shipped

- Replaced entity-memory prompt injection with top-5 recent markdown file injection in `packages/bridge/src/memory-injector.ts`.
- Added `packages/memory/src/memory-capture.ts` for append-only per-user session captures under `memory/<user>/sessions/YYYY-MM-DD-HHMM-<session-id>.md`.
- Added `packages/memory/src/memory-settings.ts` for the default-on `memory_auto_append` flag.
- Added `packages/memory/src/events.ts` with the shared `memory.path-changed` pub/sub and moved root-move notifications onto it.
- Wired `apps/desktop/src/renderer/App.tsx` to emit `memory.path-changed` on auth-driven current-user switches and to restart OpenCode on any memory path event.
- Wired `apps/desktop/src/renderer/panes/Chat/Chat.tsx` to cache the resolved active memory path, invalidate it on `memory.path-changed`, inject flat-file memory before prompts, and append verbatim captures after completed assistant replies.
- Replaced the workspace Settings placeholder with the real `panes/Settings/Settings.tsx` surface and added the automatic memory-capture toggle there.

## Verification

- `pnpm install`
- `pnpm -r typecheck`
- `pnpm -r lint`
- `pnpm -r test`

## PR

- PR #97 — `feat(memory): ship flat-file memory MVP (TIN-63, TIN-64, TIN-65)`

## Notes

- Paper MCP was not available in this workspace's tool set, so the Settings-pane change stayed minimal and token-only.
- No Rust touched, so `cargo test --lib` was not run.
