---
type: session
date: 2026-04-22
slug: tin-208
branch: khvni/tin-208-shell
pr: 121
issue: TIN-208
---

# Session — TIN-208

## Goal

Reconcile PR #106 with current MVP shell direction without reopening Playbook scope. Guard against three regressions called out in TIN-208:

- Playbook leaking into the visible titlebar chrome
- Playbook being added as a new pane kind before the TIN-203 route split
- Future agents lacking a canonical note explaining why PR #106 must stay split

## What changed

- Added a D25 application note in `agent-knowledge/product/decisions.md`:
  deferred Playbook data/model work may merge ahead of UI, but Playbook must stay out of `Workspace.tsx`, `Titlebar.tsx`, pane registration, and `packages/shared-types/src/pane.ts` until TIN-203 lands.
- Updated `agent-knowledge/features/02-playbook-skill-marketplace.md`:
  removed stale pane wording, marked the browse UI as a future full-route surface, and reset acceptance checkboxes to target-state instead of implying the feature already shipped.
- Updated `agent-knowledge/specs/2026-04-22-workspace-layout-port-design.md`:
  follow-up rail items now point to route-only integration, not new pane kinds or titlebar shortcuts.
- Added regression guards:
  - `packages/shared-types/src/pane.test-d.ts` now rejects `playbook` as an MVP pane kind / pane payload
  - `apps/desktop/src/renderer/workspace/components/Titlebar/Titlebar.test.tsx` now asserts the action cluster stays exactly the two Paper window toggles

## Verification

- `pnpm -r typecheck`
- `pnpm -r lint`
- `pnpm -r test`

## Ship state

- Draft PR opened: `feat(playbook): guard deferred shell entry points (TIN-208)` — PR #121
- TIN-208 tasks row added under post-MVP active slices as `review`
- Next follow-up lives on PR #106 itself: merge storage/runtime pieces separately from UI once route work is explicit
