---
type: session
date: 2026-04-22
topic: Merge origin/main into khvni/tin-193-195
---

# Session — merge `origin/main` into `khvni/tin-193-195`

## Why

- Pulled the latest `origin/main` into PR branch `khvni/tin-193-195` and resolved the three manual conflicts under `Titlebar` and `WorkspaceSidebar`.

## Conflict resolution

- Preserved TIN-195's MVP rail cleanup: deferred sidebar items stay removed, `Connections` stays wired through `onOpenConnections`, and `showPlaybookBadge` remains absent.
- Kept upstream TIN-200/TIN-202 polish where it still fits this branch:
  - `WorkspaceSidebar` keeps avatar image fallback, `accountLabel` tooltip, and `aria-current` state for `chat` / `memory` / `settings`.
  - `Titlebar.test.tsx` keeps crumb `title`, drag-region spacer coverage, and the double-click no-op assertion while still asserting the TIN-193 toggle behavior.

## Verification

- `pnpm --filter @tinker/desktop typecheck`
- `pnpm --filter @tinker/desktop test -- src/renderer/workspace/components/Titlebar/Titlebar.test.tsx src/renderer/workspace/components/WorkspaceSidebar/WorkspaceSidebar.test.tsx`
