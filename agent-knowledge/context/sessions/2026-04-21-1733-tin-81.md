# Session 2026-04-21 — TIN-81 Keychain Bridge
Contributor: khvni
Agent: Codex

## Scope
- Deliver [[28-mvp-identity]] task 8.8 for refresh-token keychain storage.
- Ship Tauri bridge commands, narrow bindings, verification, and tracking updates through PR-ready state.

## Changes
- Added `apps/desktop/src-tauri/src/commands/keychain.rs` with `save_refresh_token`, `load_refresh_token`, and `clear_refresh_token`.
- Keychain account keys now follow `tinker.<provider>.<user_id>` while continuing to use `tauri-plugin-keyring` as the only bearer-token store.
- Added focused Rust tests for key formatting, round-trip behavior, blank-input rejection, and an ignored live OS-keychain smoke test.
- Wired renderer permissions through `apps/desktop/src-tauri/permissions/allow-refresh-token-keychain.toml` and `capabilities/default.json`.
- Exposed typed frontend helpers in `apps/desktop/src/bindings.ts` for follow-on M8 tasks.
- Updated `agent-knowledge/context/tasks.md` row 8.8 to `review` with PR #33.

## Decisions Made
- Reused the existing keychain service name `tinker`; only the account identifier changes for per-user refresh-token entries.
- Kept the bridge narrow: no broader auth-sidecar or session-storage rewrite in this PR.
- Used a small private password-store abstraction inside `keychain.rs` to unit-test command behavior without coupling automated tests to host keychain availability.

## Knowledge Base Updates
- `agent-knowledge/context/tasks.md` — moved 8.8 to `review` and linked PR #33.
- `agent-knowledge/context/sessions/2026-04-21-1733-tin-81.md` — recorded implementation and verification details for future M8 follow-ons.

## Open Questions / Handoff
- Linux Secret Service path has an ignored live test wired via the `keyring` crate, but it was not executed in this macOS workspace.
- Existing auth session storage in `commands/auth.rs` still uses provider-only session keys; follow-on M8 tasks can decide whether to converge that storage onto the new per-user refresh-token bridge.

## Gaps Surfaced
- Workspace bootstrap was missing `node_modules`, bundled sidecar placeholders, and `resources/auth-sidecar.mjs`; local verification required restoring those environment prerequisites before tests could run.
