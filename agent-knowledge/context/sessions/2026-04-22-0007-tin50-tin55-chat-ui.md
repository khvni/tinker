---
date: 2026-04-22T00:07:00-07:00
branch: khvni/tin50-55-chat-ui
pr: 73
tickets:
  - TIN-50
  - TIN-55
---

# Session Summary

Implemented chat-pane auto-scroll polish and live context badge wiring in one PR.

## What changed

- `apps/desktop/src/renderer/panes/Chat/Chat.tsx`
  - Added sticky-bottom behavior with a 100px bottom threshold.
  - Shows a bottom-anchored `New messages` `ClickableBadge` when streaming continues while the user is scrolled up.
  - Wires `ContextBadge` into the header from the existing chat SSE subscription.
  - Seeds badge state when a session is created and refreshes it from history after prompt completion.
- `apps/desktop/src/renderer/panes/Chat/chatState.ts`
  - New local chat helper for bottom-threshold detection, tail-signature change detection, and context-badge resolution.
- `packages/bridge/src/stream.ts`
  - Added `context_usage` stream events from `message.updated` assistant payloads and `step-finish` parts so chat can reuse the same subscription instead of opening a second one.
- `apps/desktop/src/renderer/styles.css`
  - Added chat log shell + bottom pill positioning styles.

## Verification

- `pnpm -r typecheck`
- `pnpm -r test`

## Notes

- Badge resolution stores the latest raw usage snapshot so it can re-resolve after provider/model metadata finishes loading.
- No new design primitives or tokens were added; changes stay inside existing `@tinker/design` surfaces per D14/D23.
