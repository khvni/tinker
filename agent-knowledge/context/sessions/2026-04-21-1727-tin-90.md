---
type: session
date: 2026-04-21
topic: TIN-90 CI gate
---

# 2026-04-21 17:27 — TIN-90 CI gate

## Ticket

- Linear: TIN-90 — X.2 CI gate: `pnpm -r typecheck && pnpm -r test` on PRs
- Branch: `khvni/tin-90-workspace`
- PR: #29

## What shipped

- Added `.github/workflows/ci.yml`.
- Workflow runs on `pull_request` only.
- Uses `pnpm/action-setup@v4` pinned to pnpm `9.12.3`.
- Uses `actions/setup-node@v4` pinned to Node `20.11.0` with pnpm store cache from `pnpm-lock.yaml`.
- Runs:
  - `pnpm install --frozen-lockfile`
  - `pnpm -r typecheck`
  - `pnpm -r test`

## Decisions

- Kept scope to X.2 only. No Rust jobs, no lint job, no matrix, no release workflow.
- Matched ticket commands exactly instead of routing through root wrapper scripts.
- Used GitHub's built-in pnpm cache via `actions/setup-node` rather than adding extra cache plumbing.

## Review

- Reviewer pass found no adds-before-deletes problem: there was nothing to delete for this ticket, and the workflow is one file with no abstractions.
- D25 respected: cross-cutting MVP hygiene only, no post-MVP surface touched.

## Verification

- `pnpm install --frozen-lockfile`
- `pnpm -r typecheck && pnpm -r test`
- `ruby -e 'require "yaml"; YAML.load_file(".github/workflows/ci.yml")'`

## Follow-up

- GitHub branch protection still needs repo settings to mark this workflow as a required status check if maintainers want hard merge blocking on `main`.
