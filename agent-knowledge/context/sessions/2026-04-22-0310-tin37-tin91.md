---
date: 2026-04-22
branch: khvni/tin37-91-workspace
pr: 88
issues:
  - TIN-37
  - TIN-91
---

# Session — TIN-37 + TIN-91

## What shipped

- Deleted the remaining deferred renderer files at `apps/desktop/src/renderer/panes/Today.tsx` and `apps/desktop/src/renderer/panes/SchedulerPane.tsx`.
- Confirmed `Playbook.tsx` and `VaultBrowser.tsx` were already absent on `main`, so no extra `_deferred/` move or tsconfig exclusion was needed.
- Added `docs/development.md` as the manual MVP smoke checklist and linked it from `README.md` and `docs/README.md`.
- Updated `agent-knowledge/context/tasks.md` so rows `3.11` and `X.3` now point at PR #88.

## Verification

- `pnpm install`
- `pnpm -r typecheck && pnpm -r lint && pnpm -r test`

## Notes

- Linear ticket `TIN-91` called for `pnpm tauri dev`, but the workspace root does not expose that command. The runnable repo-root command is `pnpm dev:desktop`, and the smoke doc uses that instead.
- No Rust touched, so `cargo test --lib` was not run.
