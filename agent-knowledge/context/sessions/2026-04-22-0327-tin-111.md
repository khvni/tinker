---
type: session-summary
date: 2026-04-22
ticket: TIN-111
branch: khvni/tin-111
---

# 2026-04-22 03:27 — TIN-111 Enterprise SSO Okta OIDC template

## Outcome
Replaced the 13-line "Alternative: Okta with XAA" stub in `docs/enterprise-fork-guide.md` with a full Okta OIDC walkthrough that mirrors the existing Entra ID section depth. Single file, +136 / -8.

Deliverables per ticket body:
- App registration checklist (Step 2 — Native OIDC app, grant types, redirect URI registration)
- PKCE + refresh-token configuration (Step 3 — public client, S256, rotation behavior)
- Redirect URI format (Step 4 — `http://127.0.0.1:<port>/auth/callback/okta`, table covering dev / production / fork-pin scenarios)
- Better Auth config snippet (Step 6 — `genericOAuth` plugin, OIDC discovery URL, scopes)
- Scope map (Step 6 — table of `openid` / `profile` / `email` / `offline_access` with rationale + drop conditions)
- End-to-end verify checklist (Step 7) parallel to the Entra checklist
- XAA federation noted as an additive post-template adapter, not part of the identity-only template

## Why this scoping
Ticket label `post-mvp` + parent TIN-92 (Enterprise SSO milestone). D1 / D8 keep enterprise SSO out of upstream runtime, so the artifact is documentation that lives in the existing fork guide. No `packages/auth-sidecar` runtime code added.

Better Auth v1 has no built-in Okta provider. The fork wires Okta through the first-class `genericOAuth` plugin (`better-auth/plugins`) using OIDC discovery + PKCE. Documented as the canonical path so forks don't reinvent.

## Decisions
- Kept everything inside `docs/enterprise-fork-guide.md` rather than spinning a new `docs/enterprise-fork-okta-oidc.md`. Keeps a single source of truth for fork operators; matches the Entra-as-a-section pattern already established.
- Cross-linked `[[better-auth-config]]` §3, §4, §6.2, §7, §8 instead of duplicating sidecar contract details — those live in the MVP reference and forks inherit them unchanged.
- Documented Okta's exact-match redirect URI behavior (different from Entra's loose loopback matching) so forks pick the right port-pin strategy on first try.

## Out of scope (filed mentally — not new tickets yet)
- An actual `packages/auth-sidecar/src/adapters/okta-xaa.ts` adapter implementation. Belongs to the enterprise fork, not upstream. Re-entry condition: an enterprise fork operator opens an upstream PR demonstrating XAA token-exchange semantics need an upstream contract change.
- Google Workspace walkthrough has the same stub-level treatment lower in the doc and would benefit from parallel expansion. Not in TIN-111 scope; if needed, file a sibling under TIN-92.

## Tasks.md
TIN-111 is `post-mvp` — `agent-knowledge/context/tasks.md` is MVP-scoped per D25 and contains no row for this ticket. Did not add one (would create a new pattern of post-MVP rows in an MVP-only file). Status flow tracked entirely in Linear.

## Files
- `docs/enterprise-fork-guide.md` (M)
- `agent-knowledge/context/sessions/2026-04-22-0327-tin-111.md` (A)
