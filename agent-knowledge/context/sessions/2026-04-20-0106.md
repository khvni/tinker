---
type: concept
tags: [session, attention, panes]
---

# Session — 2026-04-20 01:06 — `@tinker/attention` scaffold

## Goal
Land smallest reviewable slice for `feat/attention-store`: scaffold `packages/attention` and encode Feature 12 flash/unread rules in tested store logic.

## What landed

- New package `packages/attention` with:
  - zustand vanilla store keyed by `workspaceId`
  - flash timing constants + opacity helper for cmux-style 720ms glow window
  - React hooks: `useAttentionSnapshot`, `useAttentionSignal`, `usePaneAttentionState`
  - helpers to query unread union (`collectUnreadPaneIds`, `countUnreadPanes`)
- Decision table from [[12-attention-coordinator]] encoded in unit tests:
  - notification arrival on focused vs unfocused panes
  - navigation suppression when unread/blue indicator exists elsewhere
  - manual unread dismiss + notification dismiss behavior
- Manual unread modeled separately from notification unread:
  - `unreadPaneIds` = notification-generated unread
  - `manualUnreadPaneIds` = explicit user markers
  - render unread state = union of both
- Root workspace updated so pnpm tracks new package; root `tsconfig.json` now references `packages/attention`.

## Knowledge-base updates

- `agent-knowledge/context/tasks.md`
  - Feature 12 umbrella moved to `in progress`
  - `Scaffold packages/attention + store` row moved to `review`
- `agent-knowledge/features/12-attention-coordinator.md`
  - status updated to reflect scaffold landing
  - added implementation note documenting unread union semantics + dismissal rules

## Verification

```bash
pnpm install
pnpm --filter @tinker/attention test
pnpm --filter @tinker/attention typecheck
pnpm --filter @tinker/attention lint
pnpm typecheck
```

All green.

## Next agent

- Hook `@tinker/attention` into `@tinker/panes` pane frames + tab strip (`Hook attention into @tinker/panes pane frames` task row).
- Use `collectUnreadPaneIds` / `countUnreadPanes` for pane dots and sidebar badge so manual unread survives notification dismiss.
