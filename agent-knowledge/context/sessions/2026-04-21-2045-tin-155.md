---
type: session
tags: [tin-155, tin-172, m7.8, connection-gate, ui, feat]
---

# 2026-04-21 20:45 · TIN-155 + TIN-172 ConnectionGate / ConnectionSplash

## Goal

Deliver TIN-155 (post-MVP full-window splash) while first unblocking the MVP ship-blocker slice (M7.8 minimal MCP gate). Ticket labeled `post-mvp` + row 15 of tasks.md under the D25 defer list. The ticket body explicitly says "Generalizes M7.8's MCP-only variant," so the minimal component is a genuine MVP dependency that needs to land first.

## Split

- **Option A — TIN-172** (filed this session): atomic `<ConnectionGate>` primitive in `@tinker/design`. MVP-scoped, ship-blocker for M7.8.
- **Option B — TIN-155**: `<ConnectionSplash>` composes ConnectionGate with full-window layout + Tinker wordmark + spinner + 4 service categories (host-service, auth sidecar, OpenCode, MCPs).

## What landed

- `packages/design/src/components/ConnectionGate/` (folder-per-component per D21)
  - `ConnectionGate.tsx` — pure list primitive; `services`, optional `title` (string | null to hide for embedding)
  - `ConnectionSplash.tsx` — composes ConnectionGate inside a full-window dialog with wordmark + spinner + subtitle. Flattens the nested gate's card via scoped CSS so there's no card-in-card nesting.
  - CSS: tokens-only (D14/D23), amber accent on mark + spinner sweep, `prefers-reduced-motion` opt-out on spinner
  - 10 vitest tests (5 gate, 5 splash)
- Playground entry on `/design-system` Components tab:
  - Three ConnectionGate states inline (pending / mixed / done)
  - ConnectionSplash toggled as overlay via secondary button (click dismisses)

## PRs

- [#50 — feat(design): ConnectionGate primitive (TIN-172)](https://github.com/khvni/tinker/pull/50)
- #TBD — feat(ui): ConnectionSplash full-window splash (TIN-155), stacks on #50

## Out of scope (follow-up tickets if the wire-up becomes urgent)

- M7.8 session composer wiring (needs M7.5 `mcp.status()` polling hook first)
- Host-service + auth-sidecar connection-health polling logic (full feature 15 behavior — depends on [[11-host-service]] which is deferred per D25)

## Gotchas

- TIN-155 branch existed as `byalikhani/tin-155-connection-gate-splash-component` (Linear auto-name) — discarded in favor of `feat/ui/tin-155-connection-gate-splash` per the `feat/<pillar>/<slug>` convention from CLAUDE.md §4.
- TIN-13 merge introduced a pre-existing lint error in `apps/desktop/src/renderer/workspace/chat-pane-runtime.ts:2` (`@typescript-eslint/consistent-type-imports`). Not from this session; not blocking typecheck/tests. Worth a follow-up cleanup PR.
- A pre-existing lint fail blocks `pnpm -r lint` from going green. Typecheck + tests all green (210 tests across workspace, 18 in `@tinker/design`).
