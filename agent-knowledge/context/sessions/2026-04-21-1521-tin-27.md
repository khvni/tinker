---
type: session
date: 2026-04-21
ticket: TIN-27
---

# 2026-04-21 15:21 — TIN-27 M3.1 renderer library research

## Scope

Deliver the library-selection reference document for the M3 in-line document renderer (MVP pillar per D25). Research-only ticket; no application code changes.

## Deliverable

- `agent-knowledge/reference/inline-renderers.md` — license-gated picks for PDF, XLSX, DOCX, PPTX, HTML, code, markdown with install commands, bundle estimates, and integration notes.

## Picks (summary)

- **PDF** — `pdfjs-dist` (Apache-2.0, worker required, CSP update documented).
- **XLSX** — `exceljs` (MIT). Avoided npm `xlsx` / SheetJS CE provenance ambiguity.
- **DOCX** — `mammoth` (BSD-2-Clause).
- **PPTX** — `pptxtojson` (MIT) flagged as **risky**; fallback = embedded thumbnail + external-open CTA.
- **HTML** — reuse installed `DOMPurify` inside a sandboxed `<iframe srcdoc>`; no new dep.
- **Code** — keep `highlight.js`. Evaluated Shiki; rejected on bundle cost.
- **Markdown** — keep `marked` + `DOMPurify`. Evaluated `react-markdown`; rejected on contract-churn cost.

## Notes

- Every new dep must be lazy-imported through `file-utils.ts` routing in M3.3. Non-negotiable.
- PPTX fallback decision deferred to M3.6 parity review; flagged in the doc.
- No feature file `agent-knowledge/features/22-mvp-inline-renderer.md` exists in the tree yet; the ticket references it but file creation is outside M3.1 scope.

## Next

- TIN-27 → Linear In Review.
- Follow-ups blocked by merge: M3.3 routing, M3.4/5/6 per-format panes.
