---
type: session
date: 2026-04-22
topic: TIN-197 — auto-split workspace on first file open
---

# Session — TIN-197 Ship Checklist

## What was already done (prior agent)

- Code written: `apps/desktop/src/renderer/workspace/file-open.ts` + `file-open.test.ts`
- Reviewer-approved; 2 nit edits applied (inline `findFilePaneIdInTab`, `selectWorkspaceSnapshot` round-trip test)
- Branch `khvni/tin-197` existed locally; Linear TIN-197 already In Progress + branch-name comment posted

## Verification (re-run after nit edits)

- `pnpm -r typecheck` ✅ (all 11 workspace projects)
- `pnpm -r lint` ✅
- `pnpm -r test` ✅ (235 desktop tests pass, including 13 new `file-open.test.ts` tests)
- No Rust changes; cargo test skipped per instructions

## Ship steps completed

1. **Commit** `829b58b` — `feat(m1): auto-split workspace on first file open per Paper 9I-0 (TIN-197)`
2. **Push** `khvni/tin-197` → origin
3. **PR #114** drafted + marked ready for review
4. **tasks.md** — added M1 row 1.13 for TIN-197, status `review`, PR #114 noted

## Pending (needs human or Linear CLI token)

- **Linear TIN-197 → In Review + PR link attached**: No `linear` CLI or `LINEAR_API_TOKEN` in environment. User should flip status + attach `https://github.com/khvni/tinker/pull/114`.

## Notes

- `layout.default.ts` was untouched (already spec-correct single Chat default).
- Acceptance: default split on first file open, focus-existing dedupe, non-default layout passthrough, snapshot round-trip.
