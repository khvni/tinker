# TIN-24 — Stop all sidecars on app quit

## Summary

- Added manifest reconciliation on app startup so stale manifests are adopted, verified, and cleaned before boot continues.
- Added best-effort shutdown for every manifest-tracked OpenCode sidecar on app exit.
- Simplified session-sidecar basic-auth username to a stable `tinker` value so future orphan adoption can health-check manifests deterministically.

## Files

- `apps/desktop/src-tauri/src/commands/opencode.rs`
- `apps/desktop/src-tauri/src/lib.rs`
- `agent-knowledge/context/tasks.md`

## Verification

- `cargo test --lib`
- `pnpm -r typecheck && pnpm -r test`

## Ship

- Commit: `aeb7198`
- Branch: `khvni/tin-24-ask-user`
- PR: #60
