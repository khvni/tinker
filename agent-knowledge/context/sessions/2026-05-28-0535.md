---
type: session
tags: [session, opencode, rust, tests]
---

# 2026-05-28 05:35 — Opencode Manifest + Health Tests

## Work Completed

- Updated `apps/desktop/src-tauri/tests/opencode_health_test.rs` so the integration test file is self-contained:
  - removed the undeclared `axum` dependency usage
  - stopped importing private crate internals directly
  - added a tiny local TCP HTTP responder for `/health` probe coverage
- Added a hidden `test_support::wait_for_opencode_health` wrapper in the Tauri library so integration tests can exercise the health probe without making the full `commands` module public.
- Extracted manifest binding comparison in `src/commands/opencode.rs` into `manifest_matches_binding`.
- Added unit tests for manifest matching:
  - exact folder/user/memory match
  - folder mismatch
  - user mismatch
  - memory subdir mismatch

## Verification

- Attempted `cargo check` and `cargo test` in `apps/desktop/src-tauri`.
- Both commands failed immediately because this environment has no Rust toolchain on PATH:
  - `zsh:1: command not found: cargo`
  - `rustc` and `rustfmt` are also unavailable.

## Follow-Up

- Re-run `cargo check` and `cargo test` from `apps/desktop/src-tauri` once Cargo is installed or exposed on PATH.
