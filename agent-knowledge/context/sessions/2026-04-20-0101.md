---
type: concept
tags: [session, design-system, textarea]
---

# Session — 2026-04-20 01:01 — design textarea primitive

## Goal
Ship `feat/design-textarea`: add a canonical multi-line input primitive to `@tinker/design`, migrate remaining raw textarea surfaces, and leave design-system guidance updated.

## What changed

- Added `packages/design/src/components/Textarea.tsx` + `Textarea.css`.
- Exported `<Textarea>` from `@tinker/design`.
- Updated design playground (`apps/desktop/src/renderer/routes/design-system.tsx`) to render the primitive directly and use it in the chat composer example.
- Migrated remaining renderer raw textarea call sites:
  - `apps/desktop/src/renderer/panes/Chat.tsx`
  - `apps/desktop/src/renderer/panes/SchedulerPane.tsx`
  - `apps/desktop/src/renderer/panes/Dojo.tsx`
  - `apps/desktop/src/renderer/renderers/MarkdownEditor.tsx`
- Reduced `apps/desktop/src/renderer/styles.css` textarea rules to layout-only overrides so base visual styling now lives in `@tinker/design`.

## Knowledge updates

- Added cross-cutting task row in `agent-knowledge/context/tasks.md`, moved it to `review` after verification.
- Updated `agent-knowledge/features/09-design-system.md` to include `<Textarea>`, forbid raw app-level `<textarea>` usage, and record migrated surfaces.
- Updated `agent-knowledge/README.md` UI discipline line so multi-line fields now point to `<Textarea>`.

## Verification

```bash
pnpm install --frozen-lockfile
pnpm --filter @tinker/design typecheck
pnpm --filter @tinker/desktop typecheck
pnpm typecheck
```

- `pnpm install --frozen-lockfile` — success; workspace dependencies installed because `node_modules` was absent.
- `pnpm --filter @tinker/design typecheck` — success.
- `pnpm --filter @tinker/desktop typecheck` — success.
- `pnpm typecheck` — success across packages/auth-sidecar, design, shared-types, panes, memory, scheduler, bridge, and apps/desktop.

## Follow-up notes

- `packages/design` still has no component tests; verification here is typecheck-only.
- Scheduler checkboxes remain raw `<input type="checkbox">` and are outside this branch's textarea-only scope.
