# 2026-04-21 21:35 — TIN-184 (UI.9 — Settings shell layout + section nav sidebar)

**Branch:** `khvni/settings-shell` (off `origin/main` tip `01a62cb`)
**Linear:** [TIN-184](https://linear.app/tinker/issue/TIN-184) — In Progress → In Review (once PR ready)

## Scope

Tokens-first SettingsShell primitive for the Settings pane. Rail (220px) + content region with controlled + uncontrolled active section, default EmptyPane fallback, icon slot per section. Registers the layout that TIN-61 / TIN-70 / TIN-84 / TIN-175 will fill with Account / Memory / Connections / Display.

## Files

- `apps/desktop/src/renderer/workspace/components/SettingsShell/SettingsShell.tsx` — component (controlled/uncontrolled active section, empty fallback, aria-current page, per-section icon).
- `apps/desktop/src/renderer/workspace/components/SettingsShell/SettingsShell.css` — tokens-only grid + rail + content + active/hover/focus states.
- `apps/desktop/src/renderer/workspace/components/SettingsShell/index.ts` — barrel.
- `apps/desktop/src/renderer/workspace/components/SettingsShell/SettingsShell.test.tsx` — 10 tests via `renderToStaticMarkup` (matches repo convention).
- `apps/desktop/src/renderer/routes/design-system.tsx` — new `settings-shell` tab with default / controlled / empty variants.
- `apps/desktop/src/renderer/routes/design-system.css` — playground frame + section body + card classes.

## Paper MCP fidelity

Paper file has no dedicated Settings artboard. Used the Memory view's split-rail + content pattern (`IQ-0`) as visual reference and scaled the rail (280px → 220px) for a shorter section list. Drift noted in Linear comment; tokens map cleanly so no new tokens needed.

## Token mapping

All values from `packages/design/src/styles/tokens.css`. No new tokens added.

| Purpose | Token |
|---|---|
| Rail bg | `--color-bg-panel` |
| Content bg | `--color-bg-elevated` |
| Rail border | `--color-border-subtle` |
| Nav hover | `--color-bg-hover` |
| Active nav bg | `--color-accent-soft` |
| Active icon | `--color-accent-strong` |
| Focus ring | `--color-accent-ring` |

## Verification

- `pnpm -r typecheck` → clean
- `pnpm --filter @tinker/desktop test` → 70 pass (19 files), including 10 SettingsShell tests
- `pnpm --filter @tinker/desktop dev:design` → Settings Shell tab in both light + dark themes verified via Playwright screenshots (`settings-shell-light.png`, `settings-shell-dark.png`, `settings-shell-empty-dark.png`)
- No inline hex/rgba, no new tokens, no `any`, no `@ts-ignore`

## Follow-ups

- Wire SettingsShell into `workspace/components/SettingsPane/SettingsPane.tsx` when Account (TIN-61) / Connections (TIN-70) / Display (TIN-84) / Memory settings (TIN-175) content lands.
- Optional: add arrow-key navigation across rail items once tablist semantics are decided.
