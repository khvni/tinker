---
type: session
date: 2026-04-21
ticket: TIN-89
pillar: cross-cutting (X.1)
pr: https://github.com/khvni/tinker/pull/28
---

# Session — TIN-89 async-agent onramp files

## Goal
Ship X.1 from `agent-knowledge/context/tasks.md` — add `.github/copilot-instructions.md` + `.cursor/rules/tinker.mdc` so fresh IDE / async agents land on `tasks.md` + D25 within their first 3 reads.

## What landed
- `.github/copilot-instructions.md` (22 lines) — GitHub Copilot convention, relative links.
- `.cursor/rules/tinker.mdc` (YAML frontmatter + 22-line body) — `alwaysApply: true`, `mdc:` URI scheme for file references.
- `tasks.md` row X.1 flipped `not started` → `review` with PR #28 note.

Both files use the same ordered reading list (tasks.md → decisions.md/D25 → CLAUDE.md/AGENTS.md) and the same guardrails block referenced by decision ID — no duplication of CLAUDE.md content. Supporting references section links `tinker-prd.md`, `agent-knowledge/README.md`, and the Linear workspace (team key **TIN**).

## Verification
- Reviewer agent: PASS on all five acceptance bullets. Flagged pillar-count (7 vs 8) — correct as "eight pillars" per D25's same-day amendment adding M8 Better Auth.
- `pnpm -r typecheck` green.
- `pnpm -r test` green — 145 tests across 17 files (attention 13, panes 69, bridge 10, memory 30, scheduler 3, desktop 20).

## Decisions touched
- D25 (MVP refocus) — onramp files explicitly cite D25 as the scope gate. No D25 amendment needed.
- D14 / D21 / D16 guardrails referenced by ID only, not duplicated.

## Follow-ups
None. Task was atomic S-size. No out-of-scope work identified.

## Notes for the next agent on this branch
- PR is ready for review (not draft). Waiting on human merge.
- If reviewer asks for a light-touch change, edit both files in lockstep — they're intentionally near-identical in substance. The only intentional delta is the link syntax (`mdc:` vs relative filesystem path).
- Do not amend the commit unless the human reviewer explicitly requests it.
