---
session: 2026-04-22-0730
ticket: TIN-192
branch: khvni/tin-192
pr: "#120"
---

# TIN-192 — OpenCode session binding fix (implementation)

## What shipped

5 commits on `khvni/tin-192` (based on `origin/main` @ `9c264c6`):

1. `feat(m2): idempotent start_opencode with find_live_matching + unit tests` — Rust side: `canonical_folder_path`, `find_live_matching`, early return in `start_opencode`. `OpencodeHandle` gains `username`/`password`. 3 unit tests.
2. `feat(m2): renderer state refactor` — `ReadyAppState` replaces single `opencode` with `opencodes: Record<BindingKey, OpencodeConnection>` + `defaultBindingKey`. `connectionFor()` and `defaultConnection()` helpers.
3. `feat(m2): wire folder pick to idempotent start_opencode + per-pane refcount` — `acquireOpencode` / `releaseOpencode` in `App.tsx`. Delete `restartWorkspaceOpencode`, auth-refresh effect, `RestartOpencodeOptions`. `ChatPaneRuntime` gains `getConnectionForPane` + `releaseConnectionForPane`. `RegisteredChatPane` computes per-pane connection + calls release on unmount. `Workspace` registry wraps `onSelectSessionFolder` per-pane and updates pane data.
4. `feat(m2): delete legacy opencode bootstrap + restart_opencode paths` — Remove `OpencodeState`, `OpencodeConnection`, `get_opencode_connection`, `bootstrap_opencode`, `restart_opencode`, `terminate_legacy_opencode` from `lib.rs`. Replace with `boot_home_opencode` calling `commands::opencode::start_opencode` in setup.
5. `feat(m2): pane data schema bump` — `TinkerPaneData.chat` gains `folderPath?` and `memorySubdir?`. Type tests updated.

## Verification

- `pnpm -r typecheck` green
- `pnpm -r test` green (all packages)
- `cargo test --lib` green (31 Rust tests)

## Open questions / follow-ups

- Memory path change currently re-syncs auth tokens but does not restart sidecars with new `SMART_VAULT_PATH`. File follow-up if user changes memory root while sidecars are live.
- Per-pane `vaultPath` global in `App.tsx` still exists for vault service / skill store indexing. True per-pane vault scoping deferred.

## Decisions honoured

D16 (panes registry), D17 (host/device split — manifest adoption kept), D22 (no mutate-then-call, config per-call), D25 (MVP scope), D27 (guest workspace)
