# Session 2026-04-15 — Feature 01 SSO connector layer

Contributor: anonymous
Agent: Codex GPT-5

## Scope
- Implemented [[01-sso-connector-layer]] across Tauri auth commands, renderer auth state, OpenCode connector sync, and UI status surfaces.
- Updated `knowledge/context/tasks.md` to move feature 01 to `review`.

## Changes
- Replaced Google-only OAuth command path with generic keychain-backed auth commands in `apps/desktop/src-tauri/src/commands/auth.rs`:
  - `auth_sign_in(provider)`
  - `auth_sign_out(provider)`
  - `auth_status()`
- Kept Google on loopback PKCE.
- Added GitHub device-flow auth, plus native dialog instructions with verification code.
- Added `restart_opencode` command and sidecar env injection from keychain-backed GitHub token in `apps/desktop/src-tauri/src/lib.rs`.
- Added GitHub MCP config in `opencode.json` using `@modelcontextprotocol/server-github` and `TINKER_GITHUB_TOKEN`.
- Reworked renderer auth state in `apps/desktop/src/renderer/App.tsx`:
  - multi-provider session model (`google`, `github`)
  - sidecar restart + connector resync after auth changes
  - MCP status fetch via OpenCode SDK
  - kept scheduler wiring intact while layering feature work on top
- Updated first-run, settings, workspace header, and new `IntegrationsStrip` component to show compact integration state.

## Decisions Made
- [2026-04-15] GitHub uses device flow, not loopback web flow.
  - Reason: GitHub OAuth web-flow token exchange requires `client_secret`; device flow works with `client_id` only and better fits local desktop install.
- [2026-04-15] GitHub MCP activation happens by restarting OpenCode so server env picks up `TINKER_GITHUB_TOKEN`.
  - Reason: local MCP server env is fixed at sidecar spawn time.

## Knowledge Base Updates
- `knowledge/features/01-sso-connector-layer.md`
  - status moved to `review`
  - recorded actual GitHub MCP server choice
  - documented GitHub device-flow rationale and sidecar restart behavior
- `knowledge/context/tasks.md`
  - moved feature 01 to `review`

## Open Questions / Handoff
- Google Gmail / Calendar / Drive MCP package names in seed spec still look placeholder-like. Auth layer now supports provider sync, but runtime validation still depends on real server packages/config staying valid.
- Sign-out currently restarts OpenCode for clean env reset. If active chat/session preservation matters, revisit hot-reload strategy later.
- GitHub flow needs `GITHUB_OAUTH_CLIENT_ID` configured in desktop env. Google still needs `GOOGLE_OAUTH_CLIENT_ID`.

## Gaps Surfaced
- Repo KB did not note GitHub desktop auth tradeoff between web flow and device flow.
- Repo KB still assumes Google MCP package names are settled; runtime provenance for those packages should be tightened in docs when validated.
