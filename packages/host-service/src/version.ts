/**
 * Host-service protocol version.
 *
 * Pinned manually for now. When the standalone binary ships, this will be read
 * from `packages/host-service/package.json` at build time.
 */
export const HOST_SERVICE_VERSION = '0.1.0';
