# @ramp-glass/mobile-approvals

**Owner:** W7 agent (Wave 2) · see `tasks/scheduler.md`.

Minimal React + Tailwind page served over a signed, short-lived URL. The user opens it on their phone when the headless runner pauses on a permission request, and approves or denies the pending tool call. Depends on the scheduler package being in place first.

This is a scaffolding stub. The W7 agent scaffolds the real app here.
