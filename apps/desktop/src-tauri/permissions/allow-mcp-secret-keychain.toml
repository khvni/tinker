[[permission]]
identifier = "allow-mcp-secret-keychain"
description = "Allows the renderer to save, load, and clear MCP server secrets via the OS keychain."

[permission.commands]
allow = ["save_mcp_secret", "load_mcp_secret", "clear_mcp_secret"]
deny = []
